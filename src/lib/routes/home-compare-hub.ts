/** Primary compare hub linked from homepage CTAs (single destination for consistency). */
export const HOME_COMPARE_HUB_HREF =
  "/compare/mounjaro-vs-wegovy-vs-saxenda" as const;

/** Default compare CTA label (nav / footer / cards). */
export const HOME_COMPARE_CTA_LABEL =
  "Compare weight loss treatment prices" as const;

/** Compact hero yellow button label. */
export const HOME_COMPARE_HERO_CTA_LABEL =
  "Weight Loss Treatment Prices" as const;
