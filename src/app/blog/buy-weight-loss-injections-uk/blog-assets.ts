export const BUY_INJECTIONS_CONFUSED_PNG = "buy-weight-loss-injections-confused.png";
export const BUY_INJECTIONS_SOLUTION_PNG = "buy-weight-loss-injections-solution.png";

export function blogImgPath(filename: string): string {
  return `/blog/${encodeURIComponent(filename)}`;
}
