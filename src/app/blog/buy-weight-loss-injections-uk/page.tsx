import { permanentRedirect } from "next/navigation";

const KEEP = "/blog/can-i-buy-weight-loss-injections-uk";

/** Consolidated into the canonical UK buy-injections guide. */
export default function BuyWeightLossInjectionsUkRedirect() {
  permanentRedirect(KEEP);
}
